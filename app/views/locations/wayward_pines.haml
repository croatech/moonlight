.wayward-pines
  %h1.text-center= @location.name

  .map
    .cells
      - @cells.each do |cell|
        - cache cell do
          .cell
            = link_to image_tag("locations/wayward_pines/cells/#{cell.slug}.jpg"), location_path(cell.slug)
