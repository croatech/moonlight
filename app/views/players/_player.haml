.progresses
  .progress
    .progress-bar.progress-bar-danger{"aria-valuemax" => "100", "aria-valuemin" => "0", "aria-valuenow" => "80", :role => "progressbar", :style => "width: 100%"}
      %span.sr-only 80% Complete (danger)
      = "#{@player.hp} HP"
      
  .progress
    .progress-bar.progress-bar-info{"aria-valuemax" => "100", "aria-valuemin" => "0", "aria-valuenow" => "20", :role => "progressbar", :style => "width: 100%"}
      %span.sr-only 20% Complete
      1 MP

  .progress
    .progress-bar.progress-bar-warning{"aria-valuemax" => "100", "aria-valuemin" => "0", "aria-valuenow" => "60", :role => "progressbar", :style => "width: #{progress_percents(@player.exp_next, @player.exp)}"}
      = "EXP: #{@player.exp}/#{@player.exp_next}"

.player
  .player-items.left-column
    .weapon.item
      - if @weapon.present?
        .item
          = link_to equipment_item_put_off_path(item_id: @weapon.id), method: :post do
            = image_tag @weapon.image.url

    .belt.item
      - if @belt.present?
        .item
          = link_to equipment_item_put_off_path(item_id: @belt.id), method: :post do
            = image_tag @belt.image.url

    .helmet.item
      - if @helmet.present?
        .item
          = link_to equipment_item_put_off_path(item_id: @helmet.id), method: :post do
            = image_tag @helmet.image.url

    .armor.item
      - if @armor.present?
        .item
          = link_to equipment_item_put_off_path(item_id: @armor.id), method: :post do
            = image_tag @armor.image.url

    .gloves.item
      - if @gloves.present?
        .item
          = link_to equipment_item_put_off_path(item_id: @gloves.id), method: :post do
            = image_tag @gloves.image.url

    .necklace.item
      - if @necklace.present?
        .item
          = link_to equipment_item_put_off_path(item_id: @necklace.id), method: :post do
            = image_tag @necklace.image.url

    .mail.item
      - if @mail.present?
        .item
          = link_to equipment_item_put_off_path(item_id: @mail.id), method: :post do
            = image_tag @mail.image.url

    .bracers.item
      - if @bracers.present?
        .item
          = link_to equipment_item_put_off_path(item_id: @bracers.id), method: :post do
            = image_tag @bracers.image.url

    .foots.item
      - if @foots.present?
        .item
          = link_to equipment_item_put_off_path(item_id: @foots.id), method: :post do
            = image_tag @foots.image.url

    .shield.item
      - if @shield.present?
        .item
          = link_to equipment_item_put_off_path(item_id: @shield.id), method: :post do
            = image_tag @shield.image.url

    .ring.item
      - if @ring.present?
        .item
          = link_to equipment_item_put_off_path(item_id: @ring.id), method: :post do
            = image_tag @ring.image.url

  .avatar
    = image_tag @player.image.url, class: 'avatar'


  - if @wearable_tools.present?
    - @wearable_tools.each_with_index do |item, index|
      .player-items.tools
        %div{class: "item tool num-#{index}"}
          = link_to tool_item_put_off_path(item_id: item.id), method: :post do
            = image_tag item.image.url

:javascript
  $('.player-items > #item').hover(function () {
    $(this).find('#stats').css('display', 'block');
  }, function () {
    $(this).find('#stats').css('display', 'none');
  });