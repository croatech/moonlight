.inventory.eqipment
  .col-md-6.grid.col-md-offset-1
    = render 'players/player'

  .col-md-5
    = render 'menu'

    .items
      - if @weapons.present?
        - @weapons.each do |item|
          - cache item do
            = render 'equipment/items/item', item: item
      - else
        %h2 No equipment items
